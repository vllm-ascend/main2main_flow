#
# Copyright (c) 2025 Huawei Technologies Co., Ltd. All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

name: Main2Main

on:
  schedule:
    - cron: '0 14 * * *'
  workflow_dispatch:
    inputs:
      target_commit:
        description: 'Target vLLM commit hash (default: latest vLLM main HEAD)'
        required: false
        default: ''
      model:
        description: 'opencode model name (default: deepseek/deepseek-v4-flash)'
        required: false
        default: 'deepseek/deepseek-v4-flash'

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: write

defaults:
  run:
    shell: bash -el {0}

env:
  UPSTREAM_REPO: vllm-project/vllm-ascend
  MAIN2MAIN_MODEL: ${{ github.event_name == 'workflow_dispatch' && inputs.model || 'deepseek/deepseek-v4-flash' }}
  # Accumulated adaptation state ref on HEAD_FORK, shared by resolve-source
  # (incremental mode base) and the push step (PR-success update).
  MAIN2MAIN_BASELINE_REF: main2main_baseline
  

jobs:
  resolve-source:
    runs-on: linux-amd64-cpu-8-hk
    outputs:
      source_repository: ${{ steps.source.outputs.source_repository }}
      source_ref: ${{ steps.source.outputs.source_ref }}
      base_ref: ${{ steps.source.outputs.base_ref }}
      base_sha: ${{ steps.source.outputs.base_sha }}
    env:
      TARGET_COMMIT: ${{ github.event_name == 'workflow_dispatch' && inputs.target_commit || '' }}
      HEAD_FORK: vllm-ascend-ci/vllm-ascend
    steps:
      - name: Checkout vllm-project/vllm-ascend repo
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Checkout vllm-project/vllm repo
        uses: actions/checkout@v7
        with:
          repository: vllm-project/vllm
          ref: ${{ env.TARGET_COMMIT != '' && env.TARGET_COMMIT || 'main' }}
          path: vllm-upstream
          fetch-depth: 0

      - name: Resolve fixed vllm-ascend source
        id: source
        run: |
          set -eu
          if [ -n "$TARGET_COMMIT" ] && ! echo "$TARGET_COMMIT" | grep -Eq '^[0-9a-f]{40}$'; then
            echo "::error::Invalid target_commit format: $TARGET_COMMIT"
            exit 1
          fi

          git config user.name "main2main-bot"
          git config user.email "main2main-bot@users.noreply.github.com"
          # origin is the triggering repo (= vllm-project/vllm-ascend in
          # production). Resolve the fixed upstream SHA via UPSTREAM_REPO so
          # source_ref always exists there. This fetch gates the whole run —
          # retry transient failures instead of dying before any notification
          # path exists.
          UPSTREAM_FETCH_OK=false
          for i in 1 2 3; do
            if git fetch "https://github.com/${UPSTREAM_REPO}.git" main; then
              UPSTREAM_FETCH_OK=true
              break
            fi
            echo "upstream fetch failed (attempt ${i}/3), retrying in 15s"
            sleep 15
          done
          if [ "${UPSTREAM_FETCH_OK}" != "true" ]; then
            echo "::error::upstream fetch from ${UPSTREAM_REPO} failed after 3 attempts"
            exit 1
          fi
          UPSTREAM_SHA=$(git rev-parse FETCH_HEAD)
          USE_BASELINE=false

          # Attempt incremental mode: carry forward previous adaptation commits
          # by rebasing the baseline ref onto this run's fixed upstream SHA.
          # A MISSING ref is the normal first-run state (fresh mode); a
          # transport/auth failure must not silently discard the accumulated
          # baseline — retry it and fall back to fresh mode only with a
          # loud warning.
          BASELINE_PRESENT=false
          BASELINE_FETCH_ERR=""
          for i in 1 2 3; do
            BASELINE_FETCH_ERR=$(git fetch "https://github.com/${HEAD_FORK}.git" \
              "refs/heads/${MAIN2MAIN_BASELINE_REF}:refs/remotes/main2main/baseline" 2>&1) \
              && { BASELINE_PRESENT=true; break; }
            case "${BASELINE_FETCH_ERR}" in
              *"couldn't find remote ref"*|*"does not exist"*|*"not found in"*|"")
                break ;;  # genuinely absent — no retry, fresh mode
            esac
            echo "baseline ref fetch failed (attempt ${i}/3, transport?): ${BASELINE_FETCH_ERR}"
            sleep 15
          done
          if [ "${BASELINE_PRESENT}" = "true" ]; then
            BASELINE_SHA=$(git rev-parse refs/remotes/main2main/baseline)
            BASELINE_VLLM=$(git show "${BASELINE_SHA}:.github/vllm-main-verified.commit" 2>/dev/null || true)
            TARGET_BEHIND=false
            if [ -n "$TARGET_COMMIT" ] && [ -n "$BASELINE_VLLM" ] && \
              git -C vllm-upstream merge-base --is-ancestor "$TARGET_COMMIT" "$BASELINE_VLLM" 2>/dev/null; then
              TARGET_BEHIND=true
            fi

            if [ "$TARGET_BEHIND" = false ]; then
              git checkout --detach "$BASELINE_SHA"
              if git rebase "$UPSTREAM_SHA"; then
                USE_BASELINE=true
              else
                git rebase --abort
              fi
            fi
          else
            case "${BASELINE_FETCH_ERR}" in
              *"couldn't find remote ref"*|*"does not exist"*|*"not found in"*|"")
                echo "No baseline ref ${MAIN2MAIN_BASELINE_REF} on ${HEAD_FORK} — starting in fresh mode" ;;
              *)
                echo "::warning::baseline ref fetch failed after retries (${BASELINE_FETCH_ERR}) — falling back to fresh mode; the incremental adaptation state is NOT carried forward" ;;
            esac
          fi

          # Fresh mode starts directly from the fixed upstream SHA when no
          # baseline exists, the target vLLM commit predates the baseline's
          # verified vLLM commit, or the rebase conflicts.
          if [ "$USE_BASELINE" = true ]; then
            {
              echo "source_repository=$HEAD_FORK"
              echo "source_ref=$BASELINE_SHA"
              echo "base_ref=main"
              echo "base_sha=$UPSTREAM_SHA"
            } >> "$GITHUB_OUTPUT"
          else
            {
              echo "source_repository=$UPSTREAM_REPO"
              echo "source_ref=$UPSTREAM_SHA"
              echo "base_ref="
              echo "base_sha="
            } >> "$GITHUB_OUTPUT"
          fi

  ensure-csrc-cache:
    needs: resolve-source
    uses: ./.github/workflows/_ensure_csrc_cache.yaml
    with:
      # No CPU csrc cache for this branch: the a3 cache build runner pool is
      # not among the available CPU runners, and an x86-built cache would be
      # poisoned (x86 .so saved under the hard-coded ARM64 key). The main2main
      # job compiles csrc on the a3 NPU runner instead (Install step below).
      target_ids: '[]'
      repository: ${{ needs.resolve-source.outputs.source_repository }}
      source_ref: ${{ needs.resolve-source.outputs.source_ref }}
      base_repository: vllm-project/vllm-ascend
      base_ref: ${{ needs.resolve-source.outputs.base_ref }}
      base_sha: ${{ needs.resolve-source.outputs.base_sha }}

  main2main:
    needs: [resolve-source, ensure-csrc-cache]
    runs-on: linux-aarch64-a3-16-sh-001
    timeout-minutes: 2880
    concurrency:
      group: main2main
      cancel-in-progress: false
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:9.1.0-a3-ubuntu22.04-py3.12
      env:
        VLLM_LOGGING_LEVEL: ERROR
        VLLM_USE_MODELSCOPE: True
        HCCL_BUFFSIZE: 1024
        HF_HUB_OFFLINE: 1
        UV_INDEX_URL: http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple
        UV_EXTRA_INDEX_URL: "https://repo.huaweicloud.com/ascend/repos/pypi http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu/"
        UV_INDEX_STRATEGY: unsafe-best-match
        UV_INSECURE_HOST: cache-service.nginx-pypi-cache.svc.cluster.local
        UV_HTTP_TIMEOUT: 120
        UV_NO_CACHE: 1
        UV_SYSTEM_PYTHON: 1
        PRE_COMMIT_HOME: /tmp/main2main-pre-commit
        # Use Huawei Cloud's domestic Go module proxy for pre-commit hooks
        # such as actionlint, whose golangenv otherwise accesses proxy.golang.org.
        GOPROXY: https://repo.huaweicloud.com/repository/goproxy/
        GONOSUMDB: '*'
    defaults:
      run:
        shell: bash -el {0}
    env:
      GH_TOKEN: ${{ secrets.PAT_TOKEN }}
      MAIN2MAIN_IMAGE_TAG: 9.1.0-a3-ubuntu22.04-py3.12
      TARGET_COMMIT: ${{ github.event_name == 'workflow_dispatch' && inputs.target_commit || '' }}
      PUSH_TO_GITHUB: 'true'
      GITHUB_REPO: vllm-project/vllm-ascend
      HEAD_FORK: vllm-ascend-ci/vllm-ascend
      MAIN2MAIN_KEEP_BRANCH: 'true'
      MAIN2MAIN_WORKSPACE: /tmp/main2main_flow/workspace
      # a3 runners expose 8 dual-die NPU devices: every device id must have
      # its die partner (id^1) allocated together, or two_card/four_card
      # cases land on orphan dies and fail at HCCL init.
      MAIN2MAIN_PAIR_ALIGNED_DEVICES: '1'
      # Runaway backstop only — the 20min-per-e2e-run guarantee comes from
      # case selection (test_policy.json allowlist, makespan-pinned), not
      # from this timer; the 900s hang-quiet kill fires first for silent hangs.
      MAIN2MAIN_TEST_TIMEOUT: '3600'
      MAIN2MAIN_LOG_HELPERS: |
        print_group() {
          local title="$1"
          local path="$2"
          echo "::group::${title}"
          cat "${path}" || true
          echo "::endgroup::"
        }
        print_group_if_nonempty() {
          local title="$1"
          local path="$2"
          [ -s "${path}" ] || return 0
          print_group "${title}" "${path}"
        }
    steps:
      - name: Checkout vllm-project/vllm-ascend repo
        uses: actions/checkout@v7
        with:
          repository: ${{ needs.resolve-source.outputs.source_repository }}
          ref: ${{ needs.resolve-source.outputs.source_ref }}
          fetch-depth: 0
          submodules: recursive
          persist-credentials: false

      - name: Checkout vllm-project/vllm repo
        uses: actions/checkout@v7
        with:
          repository: vllm-project/vllm
          ref: ${{ env.TARGET_COMMIT != '' && env.TARGET_COMMIT || 'main' }}
          path: vllm-upstream
          fetch-depth: 0

      - name: Install dependencies
        run: |
          sed -Ei 's@(ports|archive).ubuntu.com@cache-service.nginx-pypi-cache.svc.cluster.local:8081@g' /etc/apt/sources.list
          pip config set global.index-url http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple
          pip config set global.trusted-host cache-service.nginx-pypi-cache.svc.cluster.local
          apt-get update -y
          if [ -s "$GITHUB_WORKSPACE/packages.txt" ]; then
            xargs -r apt-get -y install < "$GITHUB_WORKSPACE/packages.txt"
          fi
          # ripgrep: the adapter's fix sessions grep with rg via bash; without
          # it each attempt burns a round on "rg: command not found" (run
          # 34084313012 step-1 attempt-1, 2 wasted calls before falling back).
          apt-get -y install git curl ca-certificates gcc g++ cmake libnuma-dev zstd clang-15 jq gh shellcheck ripgrep
          update-alternatives --install /usr/bin/clang clang /usr/bin/clang-15 20
          update-alternatives --install /usr/bin/clang++ clang++ /usr/bin/clang++-15 20
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          git config --global --add safe.directory "$GITHUB_WORKSPACE/vllm-upstream"
          pip install uv pytest

      - name: Prepare ascend workspace
        working-directory: ${{ github.workspace }}
        run: |
          set -eu
          # Keep the nested upstream checkout invisible to the ascend repo's
          # git status/diff (the flow reads the working tree diff to commit).
          echo "vllm-upstream/" >> .git/info/exclude

          # Fail fast on a malformed dispatch target instead of half an
          # hour into the run.
          TARGET="${TARGET_COMMIT}"
          if [ -n "${TARGET}" ]; then
            if ! echo "${TARGET}" | grep -Eq '^[0-9a-f]{40}$'; then
              echo "::error::Invalid target_commit format: ${TARGET}"
              exit 1
            fi
          fi

          git config user.name "main2main-bot"
          git config user.email "main2main-bot@users.noreply.github.com"
          gh --version

      - name: Check npu and CANN info
        id: check-npu
        # Detect which NPU chips are usable before the flow starts.  A
        # crashed previous job or a co-tenant holding HBM makes any e2e case
        # scheduled on those chips fail on memory (run 33944487577: chips
        # 8-15 at ~62/65GB cost 5 env_flakes and ~4h).  fuser only reaches
        # processes inside THIS container; a host-side or other-pod occupier
        # is invisible to npu-smi's process table and can only be cleaned by
        # the runner admin — the warning names the exact chips for that
        # escalation.  Partial occupancy no longer fails the run: the free
        # chips are exported and the flow schedules its e2e on them
        # (run_tests pair-aligned filtering consumes the list); only "zero
        # usable chips" is fatal.
        if: ${{ !cancelled() }}
        run: |
          set -u
          npu-smi info || true
          if [ -d /usr/local/Ascend/ascend-toolkit/latest ]; then
            cat /usr/local/Ascend/ascend-toolkit/latest/"$(uname -i)"-linux/ascend_toolkit_install.info || true
          fi
          # Kill leftover processes of THIS container holding NPU devices.
          if command -v fuser >/dev/null 2>&1 && ls /dev/davinci* >/dev/null 2>&1; then
            fuser -k /dev/davinci* 2>/dev/null || true
            sleep 5
          fi
          # Chip rows carry a PCI bus-id; $3 is the Phy-ID (= global chip
          # id 0-15, what ASCEND_RT_VISIBLE_DEVICES exposes) and the last
          # "used / total" pair is HBM-Usage(MB) — npu-smi drops the space
          # before the slash for 5-digit values ("62265/ 65536"), so the
          # separator regex must tolerate both.  An idle chip holds ~3GB
          # driver reserve; a foreign occupier showed ~62GB — threshold
          # 20GB separates the two with wide margin on both sides.
          occupied=""
          free=""
          free_count=0
          total_count=0
          while read -r chip used total; do
            [ -n "$chip" ] || continue
            [ "$total" -gt 0 ] 2>/dev/null || continue
            total_count=$((total_count + 1))
            if [ "$used" -ge 20480 ]; then
              occupied="${occupied} chip${chip}(${used}/${total}MB)"
            else
              free="${free}${free:+,}${chip}"
              free_count=$((free_count + 1))
            fi
          done < <(npu-smi info 2>/dev/null | awk '
            /[0-9A-Fa-f][0-9A-Fa-f]:[0-9A-Fa-f][0-9A-Fa-f]:[0-9A-Fa-f][0-9A-Fa-f]\.[0-9A-Fa-f]/ {
              chip = $3; line = $0; pair = ""
              while (match(line, /[0-9]+ *\/ *[0-9]+/)) {
                pair = substr(line, RSTART, RLENGTH)
                line = substr(line, RSTART + RLENGTH)
              }
              if (pair != "") {
                split(pair, kv, "/")
                gsub(/ /, "", kv[1]); gsub(/ /, "", kv[2])
                print chip, kv[1], kv[2]
              }
            }' | sort -n)
          if [ "$free_count" -eq 0 ]; then
            echo "::error::All ${total_count} NPU chips still occupied after in-container cleanup:${occupied}. These are not this container's processes — the occupier is host-side or in another pod; the runner admin must clean the host."
            exit 1
          fi
          if [ -n "$occupied" ]; then
            echo "::warning::NPU chips occupied by host-side/other-pod processes (not cleanable from here; the runner admin must clean the host):${occupied}. Degrading: the flow will schedule e2e on the ${free_count} free chip(s) [${free}] only."
          else
            echo "[check-npu] all ${total_count} chips below 20GB HBM usage - clean"
          fi
          echo "[check-npu] available devices: ${free} (${free_count}/${total_count})"
          echo "available_devices=${free}" >> "$GITHUB_OUTPUT"

      - name: Install opencode + main2main-flow
        env:
          NODE_VERSION: 24.18.0
        run: |
          set -euxo pipefail

          NODE_INSTALL_DIR="/opt/nodejs"
          NODE_ARCHIVE_NAME="node-v${NODE_VERSION}-linux-arm64.tar.gz"
          NODE_ARCHIVE="/tmp/${NODE_ARCHIVE_NAME}"
          NODE_CHECKSUMS="/tmp/node-v${NODE_VERSION}-SHASUMS256.txt"
          curl -fsSL --retry 3 --retry-delay 5 --retry-all-errors \
            -o "${NODE_ARCHIVE}" \
            "https://repo.huaweicloud.com/nodejs/v${NODE_VERSION}/${NODE_ARCHIVE_NAME}"
          curl -fsSL --retry 3 --retry-delay 5 --retry-all-errors \
            -o "${NODE_CHECKSUMS}" \
            "https://repo.huaweicloud.com/nodejs/v${NODE_VERSION}/SHASUMS256.txt"
          (cd /tmp && grep " ${NODE_ARCHIVE_NAME}$" "${NODE_CHECKSUMS}" | sha256sum -c -)
          mkdir -p "${NODE_INSTALL_DIR}"
          tar -xzf "${NODE_ARCHIVE}" --strip-components=1 -C "${NODE_INSTALL_DIR}"
          export PATH="${NODE_INSTALL_DIR}/bin:${PATH}"
          node --version
          npm --version

          npm install --global \
            --prefix "$HOME/.opencode" \
            --registry https://repo.huaweicloud.com/repository/npm/ \
            opencode-ai
          # configure opencode model provider: DeepSeek official platform
          # (OpenAI-compatible, api.deepseek.com).  MAIN2MAIN_API_KEY holds
          # the DeepSeek platform key (2026-09-07 switch from Zhipu, whose
          # catalog has no deepseek-v4-flash — run 34076411700 verify gate
          # rejected the id with "modelCode: 不存在").  Key and provider
          # must match: run 33886011133 failed auth when the yaml handed a
          # Zhipu key to this same deepseek provider.  A custom provider
          # (not the built-in catalog) works on any opencode version.
          # Strict JSON: downstream tooling rewrites this file with
          # json.loads, which would drop entries if the file were JSONC
          # with comments.
          mkdir -p ~/.config/opencode
          cat > ~/.config/opencode/opencode.jsonc <<EOF
          {
            "\$schema": "https://opencode.ai/config.json",
            "provider": {
              "deepseek": {
                "npm": "@ai-sdk/openai-compatible",
                "name": "DeepSeek",
                "options": {
                  "baseURL": "https://api.deepseek.com",
                  "apiKey": "${{ secrets.MAIN2MAIN_API_KEY }}"
                },
                "models": {
                  "deepseek-v4-flash": {
                    "name": "DeepSeek V4 Flash"
                  }
                }
              }
            }
          }
          EOF

          # ensure opencode is on PATH within this step
          export PATH="$HOME/.opencode/bin:$PATH"

          # verify opencode works
          echo "Verifying opencode..."
          set +e
          opencode run --model "${MAIN2MAIN_MODEL}" "say hello and nothing else" > /tmp/opencode_verify.log 2>&1
          OCODE_EXIT=$?
          set -e
          cat /tmp/opencode_verify.log
          if [ "${OCODE_EXIT}" -ne 0 ]; then
            echo "::error::opencode verification failed (exit ${OCODE_EXIT})"
            exit 1
          fi
          echo "opencode verification passed"

          # ensure git push uses gh token
          git config --global credential.helper "!gh auth git-credential"

          # install main2main_flow
          M2M_FLOW_DIR="/tmp/main2main_flow"
          if [ ! -d "${M2M_FLOW_DIR}/.git" ]; then
            git clone https://github.com/vllm-ascend/main2main_flow.git "${M2M_FLOW_DIR}"
          else
            # Resident runner: refresh the clone so flow fixes land without
            # manual /tmp cleanup.
            git -C "${M2M_FLOW_DIR}" fetch origin main
            git -C "${M2M_FLOW_DIR}" reset --hard origin/main
          fi
          pip install -e "${M2M_FLOW_DIR}"

      - name: Create working branch
        id: branch
        working-directory: ${{ github.workspace }}
        env:
          # The ref identifies what to fetch; the SHA fixes the exact commit
          # used as the incremental-mode rebase base.
          UPSTREAM_FETCH_REF: ${{ needs.resolve-source.outputs.base_ref }}
          UPSTREAM_REBASE_SHA: ${{ needs.resolve-source.outputs.base_sha }}
          CHECKOUT_SOURCE_SHA: ${{ needs.resolve-source.outputs.source_ref }}
        run: |
          set -eu
          BRANCH="main2main_auto_$(date +%Y-%m-%d_%H-%M)"
          UPSTREAM_BASE_SHA="${UPSTREAM_REBASE_SHA:-$CHECKOUT_SOURCE_SHA}"
          git remote add upstream "https://github.com/${UPSTREAM_REPO}.git" || \
            git remote set-url upstream "https://github.com/${UPSTREAM_REPO}.git"
          git checkout -B "$BRANCH"
          if [ -n "$UPSTREAM_REBASE_SHA" ]; then
            git fetch upstream "$UPSTREAM_FETCH_REF"
            git rebase "$UPSTREAM_REBASE_SHA"
          fi
          git cat-file -e "${UPSTREAM_BASE_SHA}^{commit}"
          # main2main_flow uses upstream/main as its squash baseline. Keep the
          # ref on the resolved snapshot instead of fetching a moving main.
          git update-ref refs/remotes/upstream/main "$UPSTREAM_BASE_SHA"
          BASE_SHA=$(git rev-parse HEAD)
          echo "name=${BRANCH}" >> "$GITHUB_OUTPUT"
          echo "base_sha=${BASE_SHA}" >> "$GITHUB_OUTPUT"

      - name: Get csrc hash
        id: get_csrc_hash
        run: |
          # Fail closed so a git error cannot silently produce sha256(empty).
          set -euo pipefail
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          cd "$GITHUB_WORKSPACE"

          # Match the standalone cache producer's tracked csrc build inputs.
          CSRC_MANIFEST=$(git ls-files -s -- csrc setup.py CMakeLists.txt cmake)
          if [ -z "$CSRC_MANIFEST" ]; then
            echo "::error::No tracked csrc inputs found."
            exit 1
          fi

          CSRC_HASH=$(printf '%s\n' "$CSRC_MANIFEST" | sha256sum | awk '{print $1}')
          echo "CSRC_HASH=$CSRC_HASH" >> "$GITHUB_OUTPUT"

      - name: Get architecture
        id: get_arch
        run: |
          ARCH=$(uname -m)
          case "$ARCH" in
            x86_64) echo "arch=X64" >> "$GITHUB_OUTPUT" ;;
            aarch64) echo "arch=ARM64" >> "$GITHUB_OUTPUT" ;;
            *) echo "arch=$ARCH" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Restore vllm-ascend csrc cache
        id: cache-csrc
        uses: runs-on/cache/restore@v5
        with:
          path: |
            vllm_ascend/_cann_ops_custom
            vllm_ascend/*.so
            vllm_ascend/lib
            vllm_ascend/include
          key: vllm-ascend-build-v2-${{ steps.get_arch.outputs.arch }}-${{ env.MAIN2MAIN_IMAGE_TAG }}-${{ steps.get_csrc_hash.outputs.CSRC_HASH }}
          # During the rollout, tolerate misses so main2main can use the existing
          # NPU build while CPU cache production proves stable.
          # Restore fail-on-cache-miss to true after the transition period.
          fail-on-cache-miss: false

      - name: Install Mooncake wheel
        run: |
          set -euxo pipefail
          apt-get install -y --no-install-recommends \
            libibverbs1 \
            ibverbs-providers \
            librdmacm1 \
            libnuma1 \
            libcurl4
          ldconfig
          MOONCAKE_WHEEL="mooncake_transfer_engine_ascend-0.3.9-cp312-cp312-manylinux_2_35_aarch64.whl"
          pip install --no-cache-dir --no-deps \
            "https://vllm-ascend.obs.cn-north-4.myhuaweicloud.com/vllm-ascend/${MOONCAKE_WHEEL}"

      - name: Install vllm-project/vllm from source
        working-directory: vllm-upstream
        run: |
          VLLM_TARGET_DEVICE=empty uv pip install .
          pip uninstall -y triton

      - name: Install vllm-project/vllm-ascend
        working-directory: ${{ github.workspace }}
        env:
          CSRC_CACHE_HIT: ${{ steps.cache-csrc.outputs.cache-hit }}
          # a3 SoC values for the on-runner compile (this branch runs without
          # a CPU-produced csrc cache) — do not derive from the runner name's
          # trailing digits.
          SOC_VERSION: ascend910_9391
          MAX_JOBS: '184'
        run: |
          pip install uc-manager
          uv pip install -r requirements-dev.txt
          uv pip install --force-reinstall --no-deps triton-ascend==3.2.2
          # Preserve main2main availability during the transition by compiling
          # on the NPU only when the CPU-produced cache is unavailable.
          if [ "$CSRC_CACHE_HIT" = "true" ]; then
            echo "csrc cache hit: skip kernel compilation"
            COMPILE_CUSTOM_KERNELS=0 uv pip install -e .
          else
            echo "csrc cache miss: compile kernels on the NPU runner"
            uv pip install -e . --no-build-isolation
          fi

      - name: Save vllm-ascend csrc cache
        # Mirror vllm-ascend main CI: after an on-runner compile (cache
        # miss), persist the built artifacts under the restore step's
        # primary key so the next dispatch hits instead of recompiling.
        if: steps.cache-csrc.outputs.cache-hit != 'true'
        uses: runs-on/cache/save@v5
        with:
          path: |
            vllm_ascend/_cann_ops_custom
            vllm_ascend/*.so
            vllm_ascend/lib
            vllm_ascend/include
          key: vllm-ascend-build-v2-${{ steps.get_arch.outputs.arch }}-${{ env.MAIN2MAIN_IMAGE_TAG }}-${{ steps.get_csrc_hash.outputs.CSRC_HASH }}

      - name: Run main2main flow
        id: run-main2main
        working-directory: ${{ github.workspace }}
        env:
          # Free chips only (from check-npu): run_tests' _detect_cards reads
          # this list as the physical device pool, and every child process
          # (warmup, e2e) inherits the same restriction.
          ASCEND_RT_VISIBLE_DEVICES: ${{ steps.check-npu.outputs.available_devices }}
          RAY_EXPERIMENTAL_NOSET_ASCEND_RT_VISIBLE_DEVICES: True
        run: |
          set -euo pipefail
          export PATH="$HOME/.opencode/bin:$PATH"
          eval "${MAIN2MAIN_LOG_HELPERS}"

          rm -rf /tmp/main2main
          mkdir -p /tmp/main2main

          cat > /tmp/main2main/heartbeat.sh <<'HEARTBEAT'
          trap "exit 0" TERM INT
          printf '\n::notice::main2main heartbeat %s\n' "$(date -Is)"
          while sleep 300; do
            printf '\n::notice::main2main heartbeat %s\n' "$(date -Is)"
          done
          HEARTBEAT
          setsid bash /tmp/main2main/heartbeat.sh &
          HEARTBEAT_PID="$!"
          cleanup_heartbeat() {
            if [ -n "${HEARTBEAT_PID:-}" ]; then
              kill -TERM -- "-${HEARTBEAT_PID}" 2>/dev/null || true
              wait "${HEARTBEAT_PID}" 2>/dev/null || true
            fi
          }
          trap cleanup_heartbeat EXIT

          set +e
          kickoff \
            --vllm-path "$GITHUB_WORKSPACE/vllm-upstream" \
            --vllm-ascend-path "$GITHUB_WORKSPACE" \
            --target-commit "${TARGET_COMMIT}" \
            2> /tmp/main2main/kickoff.err \
            | tee /tmp/main2main/kickoff.log
          KICKOFF_STATUS="${PIPESTATUS[0]}"
          set -e
          cleanup_heartbeat
          trap - EXIT

          echo "::notice::after kickoff: $(date -Is)"
          echo "KICKOFF_STATUS=${KICKOFF_STATUS}"
          ls -la /tmp/main2main || true
          find /tmp/main2main -maxdepth 3 -type f -print || true

          print_group "main2main kickoff output" /tmp/main2main/kickoff.log
          print_group_if_nonempty "main2main kickoff stderr" /tmp/main2main/kickoff.err

          if [ "${KICKOFF_STATUS}" -ne 0 ]; then
            echo "::error::kickoff exited with status ${KICKOFF_STATUS}"
            exit "${KICKOFF_STATUS}"
          fi

          # Copy final summary from workspace
          FINAL_SUMMARY="${MAIN2MAIN_WORKSPACE}/final_summary.md"
          if [ -f "${FINAL_SUMMARY}" ]; then
            cp "${FINAL_SUMMARY}" /tmp/main2main/final-summary.md
          fi

          if [ -s /tmp/main2main/final-summary.md ]; then
            print_group "main2main final summary" /tmp/main2main/final-summary.md
          else
            echo "::warning::final-summary.md not found, generating stub"
            echo "Status: completed" > /tmp/main2main/final-summary.md
            echo "Steps: 0/0" >> /tmp/main2main/final-summary.md
          fi

      - name: Upload workspace
        if: ${{ !cancelled() }}
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: main2main-workspace
          path: /tmp/main2main_flow/workspace/
          retention-days: 7

      - name: Summarize final status
        id: final-status
        working-directory: ${{ github.workspace }}
        if: ${{ !cancelled() }}
        run: |
          set -eu
          eval "${MAIN2MAIN_LOG_HELPERS}"

          STATUS_JSON="${MAIN2MAIN_WORKSPACE}/final_status.json"
          BASE_SHA="${{ steps.branch.outputs.base_sha }}"

          # kickoff may exit 0 without writing final_status.json (e.g. flow short-circuits
          # on no new commits). Degrade gracefully instead of crashing on FileNotFoundError.
          if [ ! -f "${STATUS_JSON}" ]; then
            echo "::warning::final_status.json not found at ${STATUS_JSON}; kickoff produced no structured status"
            FINAL_STATUS="missing"
            OLD_COMMIT=""
            NEW_COMMIT=""
            REACHED_COMMIT=""
            STEPS_COMPLETED="0"
            STEPS_TOTAL="0"
          else
            OLD_COMMIT=$(python3 -c "import json; print(json.load(open('${STATUS_JSON}'))['old_commit'])")
            NEW_COMMIT=$(python3 -c "import json; print(json.load(open('${STATUS_JSON}'))['new_commit'])")
            FINAL_STATUS=$(python3 -c "import json; print(json.load(open('${STATUS_JSON}'))['status'])")
            REACHED_COMMIT=$(python3 -c "import json; print(json.load(open('${STATUS_JSON}'))['reached_commit'])")
            STEPS_COMPLETED=$(python3 -c "import json; print(json.load(open('${STATUS_JSON}'))['steps_completed'])")
            STEPS_TOTAL=$(python3 -c "import json; print(json.load(open('${STATUS_JSON}'))['steps_total'])")
          fi

          MANUAL_REVIEW_REQUIRED=false
          if [ "${FINAL_STATUS}" != "completed" ]; then
            MANUAL_REVIEW_REQUIRED=true
          fi

          # created-commits.md requires base_sha and a valid repo; guard earlier-step failures.
          if [ -n "${BASE_SHA}" ] && git rev-parse --verify HEAD >/dev/null 2>&1; then
            git log --reverse --format="- \`%H\` %s" "${BASE_SHA}..HEAD" \
              > /tmp/main2main/created-commits.md || echo "(git log failed)" > /tmp/main2main/created-commits.md
            COMMIT_COUNT=$(git rev-list --count "${BASE_SHA}..HEAD" 2>/dev/null || echo 0)
          else
            echo "(base_sha unavailable or not a git repo)" > /tmp/main2main/created-commits.md
            COMMIT_COUNT=0
          fi

          {
            echo "status=${FINAL_STATUS}"
            echo "commit_count=${COMMIT_COUNT}"
            echo "steps_completed=${STEPS_COMPLETED}"
            echo "manual_review_required=${MANUAL_REVIEW_REQUIRED}"
            echo "old_commit=${OLD_COMMIT}"
            echo "new_commit=${NEW_COMMIT}"
          } >> "$GITHUB_OUTPUT"
          echo "::notice::final_status=${FINAL_STATUS}, reached_commit=${REACHED_COMMIT}, steps=${STEPS_COMPLETED}/${STEPS_TOTAL}, commit_count=${COMMIT_COUNT}"
          print_group_if_nonempty "main2main created commits" /tmp/main2main/created-commits.md

      - name: Create manual review issue
        if: steps.final-status.outputs.manual_review_required == 'true'
        working-directory: ${{ github.workspace }}
        run: |
          eval "${MAIN2MAIN_LOG_HELPERS}"

          PR_URL=$(cat /tmp/main2main/pr_url.txt 2>/dev/null || echo "")
          OLD="${{ steps.final-status.outputs.old_commit }}"
          NEW="${{ steps.final-status.outputs.new_commit }}"
          SHORT_NEW=$(printf '%.8s' "${NEW}")

          cat > /tmp/main2main-manual-review.md <<EOF
          ## Summary

          main2main automation stopped before completing all planned steps.

          ## Context

          - Draft PR: ${PR_URL}
          - Commit range: \`${OLD}\`...\`${NEW}\`
          - Status: \`${{ steps.final-status.outputs.status }}\`

          ## Final Summary

          $(cat /tmp/main2main/final-summary.md 2>/dev/null || echo "(final summary unavailable)")
          EOF

          print_group "Manual review issue" /tmp/main2main-manual-review.md
          gh issue create \
            --repo "${UPSTREAM_REPO}" \
            --title "[main2main] main2main manual review required (${SHORT_NEW})" \
            --body-file /tmp/main2main-manual-review.md

      - name: Chain next run
        if: steps.final-status.outputs.status == 'completed' && steps.final-status.outputs.steps_completed != '0'
        run: |
          echo "::notice::Chaining next main2main run..."
          gh workflow run Main2Main \
            --repo "${UPSTREAM_REPO}"
